import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  cards: {
    marginTop: 20,
    width: 350,
    height: 200,
    backgroundColor: 'white',
    alignItems: 'center',
    borderRadius: 40,
  },
});
